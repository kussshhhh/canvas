name = "physical-compiler-api"
main = "app/main.py"
compatibility_date = "2024-01-01"
compatibility_flags = ["python_workers"]

[vars]
# GEMINI_API_KEY should be set via Cloudflare Dashboard for security, 
# but you can also put a placeholder here.
GEMINI_API_KEY = "PLACEHOLDER_SET_IN_DASHBOARD"
